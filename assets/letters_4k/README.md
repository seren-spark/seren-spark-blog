# 提取后的字母素材

每个来源文件夹内是按字母命名的透明 PNG，统一为 `2048 × 2048`，保留原图颜色与透明背景，并做了轻微锐化。

- `图6/`、`图5/`、`图3/`、`图层0/`：A–Z，共 26 个字母
- `图2/`：A–Y，共 25 个字母（原图中没有单独的 Z）
- `sheets/`：五张放大版整图，最长边为 3840 像素
- `contact_sheets/`：用于快速查看的缩略预览
- `manifest.json`：输出清单
